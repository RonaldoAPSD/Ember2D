This text shows the current issues with the engine after testing the engine further.

[Major issues]
1. Whenever there is a panel opened if you edit anything on the pannel while the painter is selected it will paint on the the level editor.
2. When opening anything on the top bar such as file, edit, ect... pannels will cover it making it unable to be read.
3. You can paint outside of grid size if the screen is big enough. Not sure what to do about this. If we should just prevent painting outside of grid size? but that could get confusing because a user might think they can just paint out there to extend the grid size and then get confused. Not sure how to resolve this.

[Minior issues]  
5. Set spawn on the menu does not prompt you to select where to set spawn it just goes where ever your mouse cursor seems to be after you click it which is often in the same spots.
6. Level editor does not support zooming in and out with mouse wheel it instead scrolls down big grid files which i can already pan by clicking middle mouse buttons and using arrow keys so Id rather be able to zoom in and out.
